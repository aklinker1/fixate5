<?xml version="1.0" encoding="utf-8"?>
<merge
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <androidx.appcompat.widget.AppCompatImageView
        android:id="@+id/icon"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_gravity="center_vertical"
        android:layout_marginStart="8dp"
        android:tint="@color/main_menu_item_foreground_color"
        android:tintMode="multiply"
        tools:src="@drawable/nav_home" />

    <io.aklinker1.files.ui.WeightedTextView
        android:id="@+id/title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center_vertical"
        android:layout_marginStart="56dp"
        android:layout_marginEnd="16dp"
        android:textColor="@color/main_menu_item_foreground_color"
        android:textSize="@dimen/main_menu_item_text_size"
        tools:text="File Explorer" />

</merge>
